{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/railway/Dockerfile.app",
    "watchPatterns": [
      "package.json",
      "bun.lock",
      ".dockerignore",
      "Dockerfile",
      "apps/app/**",
      "packages/**",
      "deploy/railway/Dockerfile.app"
    ]
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
